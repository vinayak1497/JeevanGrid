# ── Production API base ──────────────────────────────────────
# Local dev: leave empty (Vite proxies /api → http://localhost:5000).
# Vercel: set to your deployed backend URL, e.g.
# VITE_API_URL=https://jeevangrid-api.onrender.com/api
VITE_API_URL=
